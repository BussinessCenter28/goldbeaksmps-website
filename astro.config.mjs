// @ts-check
import { defineConfig } from 'astro/config';

// Site URL = your custom domain on GitHub Pages.
// Because a custom domain serves from the root, no `base` is needed.
export default defineConfig({
  site: 'https://goldbeaksmps.asmp.info',
});
